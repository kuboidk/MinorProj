using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class upwego : MonoBehaviour
{
    public float upspeed = 1f;
    public float x = 1f;
    public float y = 1f;
    // Start is called before the first frame update\
  // public kubo inventory;
 //   public GameObject kunai;
    void Start()
    {
        //  inventory = gameObject.findGameoblectw
      //  inventory = GameObject.Find("kubo").GetComponent<kubo>();
    }

    // Update is called once per frame
    void Update()
    {


       // if (col.gameObject.name == "Kubo")
       // {
       ///     for (int i = 0; i < inventory.slot.Length; i++)
          //  {
            //    if (inventory.isfull[i] == false)
             //   {
                  //  Instantiate(kunai, inventory.slot[i].transform, false);
               //     //     gameObject.transform.parent = inventory.slot[i].transform;

             //   }
              //  inventory.isfull[i] = true;
             ///   break;
          //  }
      ////  }
       // transform.position = new Vector3(x, Mathf.PingPong(Time.time, upspeed) +y, 0f);
    }
   // void OnTriggerEnter2D(Collider2D col)
   // {
     //   if (col.gameObject.name == "Kubo")
      //  {
       //     for (int i = 0; i < inventory.slot.Length; i++)
        //    {
           //     if (inventory.isfull[i] == false)
          //      {
              //      Instantiate(kunai, inventory.slot[i].transform, false);
                    //     gameObject.transform.parent = inventory.slot[i].transform;

            //    }
            //    inventory.isfull[i] = true;
           //     break;
          //  }
      //  }

// }
}
